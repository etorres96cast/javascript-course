const productoA = 'monitor de 20 pulgadas';

//propiedades
//determina el numero de caracteres
productoA.length;
console.log(productoA.length);

//metodo de strings
//busca en que posicion se encuentra el string que pasemos como parametros
productoA.indexOf('pulgadas');
console.log(productoA.indexOf('pulgadas'));

//busca si el string que le pasemos como parametro se encuentra
//devuelve true o false
productoA.includes('pulgadas');
console.log(productoA.includes('pulgadas'));