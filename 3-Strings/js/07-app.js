const productTestA = 'Monitor de 20 pulgadas';

//.toUpperCase(), todo el texto lo coloca en mayuscula
console.log(productTestA.toUpperCase());

//.toLowerCase(), todo el texto lo coloca en minuscula
console.log(productTestA.toLowerCase());

//si se desea que dicha variable, el numero que lo contiene sea texto
const precio = 300;
console.log(precio);

//se debe aplicar lo sigueinte:
const newValue = precio.toString();

console.log(newValue);
